---
title: "API reference"
description: "Example REST API documented from an OpenAPI spec."
---

This API reference is generated from OpenAPI specifications. Each endpoint is
rendered automatically — edit the spec to change the docs.

## Versions

<CardGroup cols={2}>
  <Card title="API v1" icon="code-branch">
    The base Items API — list, create, get, and delete items.
    Spec: `api-reference/v1/openapi.json`
  </Card>
  <Card title="API v2" icon="code-branch">
    Adds pagination, `createdAt`/`updatedAt` timestamps, and a `PATCH` update endpoint.
    Spec: `api-reference/v2/openapi.json`
  </Card>
</CardGroup>

## Authentication

All endpoints are authenticated with a bearer token:

```bash
Authorization: Bearer <token>
```

## Base URL

```
https://api.example.com/v1
```
