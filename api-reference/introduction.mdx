---
title: "API reference"
description: "Example REST API documented from an OpenAPI spec."
---

This API reference is generated from the OpenAPI specification at
`api-reference/openapi.json`. Each endpoint below is rendered automatically —
edit the spec to change the docs.

## Authentication

All endpoints are authenticated with a bearer token:

```bash
Authorization: Bearer <token>
```

## Base URL

```
https://api.example.com/v1
```
